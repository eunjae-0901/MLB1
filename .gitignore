.venv/
__pycache__/
*.pyc
.claude/

# Scenario outputs that are large and reproducible from tracked scripts.
**/data/case_control/*_dataset_*.csv
**/scenarios/**/results/models/
**/scenarios/**/results/*predictions*.csv
**/scenarios/**/data/

# Kang 2025 reproduction intermediates and GPU checkpoints.
베이스라인 파일/data/*.npz
베이스라인 파일/results/checkpoints/
베이스라인 파일/results/*predictions*.csv

# 필요없는거/ 는 예전 파이프라인 전체를 "혹시 몰라서" 남겨둔 백업 폴더.
# 스크립트(노트북)만 참고용으로 추적하고, 용량 큰 원본/중간 데이터(raw+interim+
# export+processed 합쳐서 1GB+)와 예전 모델 산출물(.json/.pt/.xlsx)은 재생성
# 가능하고 지금 안 쓰는 거라 git에는 올리지 않음.
필요없는거/mlb_injury_project/data/
필요없는거/mlb_injury_project/models/

# 당장 필요한 파일/ 노트북을 실행할 때 생기는 로그/결과 산출물 - 코드가 아니라
# 실행 결과물이라 git에는 안 올림 (노트북 다시 돌리면 재생성됨)
당장 필요한 파일/*_run_log.txt
당장 필요한 파일/*_results.json
